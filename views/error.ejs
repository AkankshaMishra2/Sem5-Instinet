<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - InstiNet</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .error-container {
            min-height: 80vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 20px;
        }
        
        .error-icon {
            color: var(--danger-color);
            margin-bottom: 20px;
        }
        
        .error-title {
            font-size: 2.5rem;
            color: var(--dark-color);
            margin-bottom: 15px;
        }
        
        .error-message {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 30px;
            max-width: 600px;
        }
        
        .error-actions {
            display: flex;
            gap: 15px;
        }
    </style>
</head>
<body>
    <header class="main-header">
        <div class="logo">
            <h1>InstiNet</h1>
        </div>
        <nav class="main-nav">
            <ul>
                <li><a href="/">Home</a></li>
                <% if (typeof user !== 'undefined') { %>
                    <li><a href="/logout">Logout</a></li>
                <% } else { %>
                    <li><a href="/login">Login</a></li>
                    <li><a href="/register">Register</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    
    <div class="container">
        <div class="error-container">
            <div class="error-icon">
                <i class="fas fa-exclamation-triangle fa-5x"></i>
            </div>
            <h1 class="error-title"><%= status %> - <%= message %></h1>
            <p class="error-message">
                <%= description || "We're sorry, but something went wrong. Please try again later or contact support if the issue persists." %>
            </p>
            <div class="error-actions">
                <a href="/" class="btn btn-primary"><i class="fas fa-home"></i> Go Home</a>
                <a href="javascript:history.back()" class="btn btn-outline"><i class="fas fa-arrow-left"></i> Go Back</a>
            </div>
        </div>
    </div>
    
    <%- include('./partials/footer') %>
    
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>